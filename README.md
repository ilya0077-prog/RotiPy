# RotiPy
An OOP pipeline that parses .cif and .pdb files from protein databases (such as AlphaFold) and calculates rotifer peptide and target residues. It generates structural data utilizing BioPython hierarchies in order to print residues to approximate docking compatibility in ChimeraX or Mol*.
